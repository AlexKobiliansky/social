import React from "react";
import styles from './ProfileHead.module.css'

function ProfileHead() {
    return (
        <div>
            img + photo + name + interests
        </div>
    )
}

export default ProfileHead;